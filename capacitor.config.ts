import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jayner1.fooddudefresh',
  appName: 'FoodDudeFresh',
  webDir: 'build'
};

export default config;
